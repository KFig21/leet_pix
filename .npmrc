# Railway/Nixpacks builds with NODE_ENV=production, which makes `npm ci` omit
# devDependencies. But building the server needs dev tooling (tsup, prisma,
# typescript), so force dev deps to install in every environment. Without this
# the deploy fails with "tsup: not found".
include=dev
