{
  "name": "leetpix",
  "version": "0.0.0",
  "private": true,
  "description": "Social platform for fantasy sports picks polls",
  "workspaces": [
    "packages/*",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w server && npm run build -w client",
    "lint": "npm run lint -w server && npm run lint -w client",
    "db:generate": "npm run db:generate -w server",
    "db:migrate": "npm run db:migrate -w server",
    "db:studio": "npm run db:studio -w server",
    "db:seed": "npm run db:seed -w server",
    "import:players": "npm run import:players -w server",
    "import:stats": "npm run import:stats -w server",
    "resolve:poll": "npm run resolve:poll -w server",
    "resolve:due": "npm run resolve:due -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
